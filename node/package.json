{
  "name": "inp-legend-backend",
  "version": "1.0.0",
  "scripts": {
    "start": "npx nodemon app.js",
    "test": "node app.js",
    "build": "node app.js",
    "rebuild-image": "docker rmi -f app_image || true && docker build -t app_image .",
    "setup": "npm install && npm run rebuild-image"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dockerode": "^4.0.2",
    "dotenv": "^16.4.5",
    "express": "^4.19.1",
    "mysql": "^2.18.1",
    "jws": "^4.0.0"
  }
}
